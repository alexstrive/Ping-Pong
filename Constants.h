#include <string>

using namespace std;

// Properties of window
// Maybe later i renamed it to WINDOW_WIDTH and WINDOW_HEIGHT
const int WINDOW_WIDTH = 800;
const int WINDOW_HEIGHT = 600;

const string GAME_VERSION = "1.0";
